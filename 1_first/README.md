# Chapter 1
## a 태그 vs Link 태그
- a 태그를 사용 시 기존의 MPA(다중 페이지 앱)처럼 동작함.
    1. url을 입력했을 때 브라우저에서 서버로부터 만들어진 HTML 파일을 다운로드함.
    2. 브라우저가 HTML을 분석해 렌더링에 필요한 css, js 등 파일들을 다운로드함.
    3. 브라우저가 렌더링에 필요한 파일들을 다운 받고 돔을 구성함.
    4. 리액트가 하이드레이션(Hydration)을 수행.    
        하이드레이션은 브라우저가 렌더링된 일반적인 돔에 리액트 가상 돔으로 동기화해 상호작용이 가능한 상태로 만드는 것을 의미함.

   a 태그로 화면이 전환될 땐 1번부터 4번을 계속 반복함으로 하이드레이션된 리액트가 없어짐.
- next/link 모듈의 Link 태그를 사용하시면 단계별로 동작함.
    1. 맨 처음 앱에 들어왔을 때 
        1. a 태그와 똑같이 동작.
    2. Link 태그를 사용해 페이지 전환 시    
        1. Nextjs의 라우터가 반응함.
        2. 서버에서 미리 prefetch한 JSON 데이터 사용
        3. 리액트의 가상 돔에서 컨포넌트를 교체해 렌더링.
        4. URL만 바뀌고, 새로고침 없음.
 
    이렇게 동작하며 SSR의 초기 로딩 속도와 SEO 장점을 유지하며 CSR의 사용자에 친화적인 UX를 제공.